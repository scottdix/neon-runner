schema_version: 1
# Solo project manifest — the session-memory heartbeat.
# Read at /session-start, advanced at /handoff. History depth is 2
# (notes + previous_notes); older history lives in git + docs/handoff/.
last_updated:
  date: "2026-06-20"
  session: 5
  notes: >
    Session 5 (big one). (1) RENAMED game "Neon Runner"->"Neon Splice" (App Store
    name conflict); bundle id com.scottdix.neonsplice registered with Apple (repo slug
    stays neon-runner). (2) Reconciled the FULL scope -> docs/design/GAME_SCOPE.md, now
    authoritative (CLAUDE.md points to it): the game is a free-steer, continuous-fire,
    vector bullet-hell survival shooter, NOT the old endless lane-runner; 4 locked
    decisions (analog steer, one win/loss, two collision models D3, authored phases).
    (3) Tracker 52->64 issues: enemy epic #53 + #54-#63 + #64 (Bazzite/WoL); retitled
    #9/#10/#44; reconciliation comments on stale issues. (4) FIXED the dev loop: the
    installed _mono Godot hangs at headless startup -> installed STANDARD Godot 4.7 +
    reusable harness (tools/run-headless.sh, verify_events.gd, screenshot.gd). (5) #3
    Events autoload VERIFIED + CLOSED (was blocked 2 sessions; it was the mono binary).
    (6) #6 POC built (assets/poc/glow_stress): MultiMesh fleet + batched collision;
    headless PASS, logic flat ~30us across a 10x fleet (4k->40k) = D3 proven; glow/FPS
    NOT renderable on this mini (Intel UHD 630). (7) Built the FULL iOS/TestFlight
    pipeline (fastlane/, signing cert+keychain+profile, Godot iOS export -> valid Xcode
    project; PLA accepted). BLOCKED at xcodebuild archive: Godot 4.7 needs Xcode 26 /
    iOS 26 SDK (engine refs MTLTensorDomain/CADynamicRange absent from Xcode 16.4).
    Toolchain upgrade (mini->Tahoe+Xcode26, uncertain on Intel; or build on the M2 Air)
    deferred to next session by the user.
  previous_notes: >
    Session 4: Design-led planning (no gameplay code). Imported the first art-direction
    pass into docs/design/DESIGN_SPEC.md (palette, 4 fonts, 4 screens). TWO CORE
    DECISIONS LOCKED: FINITE distance-based levels w/ FINISH line (not endless), and
    FIRING is a CORE mechanic. Monetization = paymium (no ads, one-time IAP). New issues
    #47-#52; re-scoped #44/#41/#42/#9/#29/#10/#12/#13. POC fast-path agreed (#6 + #47).

# =============================================================================
# FOCUS — one milestone, one issue at a time
# =============================================================================
focus:
  active_milestone: "v0.1.0 - Proof of Concept"
  active_epic: "#1 — Phase 1-2: Foundation & Project Architecture"
  current_issue: "#47 — iOS build + on-device deploy (BLOCKED: Godot 4.7 needs Xcode 26 / iOS 26 SDK)"
  next_issue: "MVP gameplay slice (#9/#10/#52 steer+fire, #11/#56 gates, #55 Glow Battery, #51 finish) — NOT device-gated"
  notes: >
    #3 DONE/closed (Events verified). #6 POC built + headless-validated (D3 proven); its
    device visual/perf check is gated by #47. #47 is BLOCKED purely on the Xcode/SDK
    version: the whole TestFlight pipeline (fastlane/, signing, profile, Godot export)
    works — only `xcodebuild archive` fails for lack of the iOS 26 SDK. RESOLVE by
    getting Xcode 26 onto a usable Mac: (a) mini -> macOS 26 Tahoe + Xcode 26 (Intel
    uncertain, ~67GB free), or (b) the MacBook Air M2 (Apple Silicon, clean; 8GB tight).
    Then: fastlane ios make_profile -> verify regenerated scheme -> fastlane ios ship ->
    create ASC app record in web UI (name "Neon Splice", bundle com.scottdix.neonsplice).
    ALTERNATIVELY start the MVP gameplay slice (builds/validates on standard Godot now;
    visuals on the M2 Air/VNC). Signing artifacts + .p8 path: see SESSION-005 handoff.

# =============================================================================
# TRACKER
# =============================================================================
# GitHub Issues is the canonical task tracker (gh CLI).
#   Epics:      type: epic   (one per implementation-plan phase, + enemy faction #53)
#   Milestones: v0.1.0 ... v1.0.0 (the version ladder)
#   Labels:     type / priority / effort / status / area
tracker:
  kind: github-issues
  repo: scottdix/neon-runner

# =============================================================================
# PROJECT REFERENCE
# =============================================================================
# Engine:  Godot 4.7 (STANDARD/non-mono build for the GDScript dev loop), GDScript-first
# Scope:   docs/design/GAME_SCOPE.md (authoritative — what the game is)
# Look:    docs/design/DESIGN_SPEC.md  ·  Plan/stubs: IMPLEMENTATION_PLAN.md (superseded appendix)
