schema_version: 1
# Solo project manifest — the session-memory heartbeat.
# Read at /session-start, advanced at /handoff. History depth is 2
# (notes + previous_notes); older history lives in git + docs/handoff/.
last_updated:
  date: "2026-06-22"
  session: 7
  notes: >
    Session 7. (1) TESTFLIGHT INSTALLABLE: build #1 was packaged iPad-only (Godot
    targeted_device_family=1 == iPad in its enum) -> "incompatible" on the iPhone 12 Pro.
    Fixed to 2 (iPhone & iPad, UIDeviceFamily [1,2]); created internal TestFlight group +
    tester (scottdix@gmail.com, no dot) via ASC API. (2) #6 POC DEVICE-VALIDATED: glow blooms,
    FPS 59-60 on the iPhone 12 Pro -- the last check the Intel mini couldn't do. (3) BUILT THE
    MVP GAMEPLAY SLICE (#9/#10/#52, partial #14), device-validated: new GameState autoload (#2),
    Events-bus extensions, new Run main scene (replaces the POC) = analog slide-steer + always-on
    MultiMesh fire stream + shootable exploding targets + HUD; all headless-validated
    (tools/verify_run.gd) + clean boot. (4) Iterated game-feel over builds 2->7 to a CONNECTED
    hit loop: glow render-path fix (ship/enemies via MultiMesh/additive HDR, NOT draw_* which
    never blooms), then PER-IMPACT DAMAGE (bullets consumed on contact deal the damage) + death
    bursts -> shoot->impact->destroy confirmed on device. (5) Gotchas: ship-over-SSH needs
    keychain unlock + set-key-partition-list (build #2 failed errSecInternalComponent without
    it); main scene can't use bare class_name (no import cache) -> preload().
  previous_notes: >
    Session 6. (1) iOS TOOLCHAIN UNBLOCKED + #47 SHIPPED. The session-5 plan (mini -> macOS
    Tahoe) was impossible (Tahoe drops the 2018 mini) AND unnecessary: Xcode 26's floor is
    Sequoia 15.6 (mini runs 15.7.7) and Apple ships a Universal Xcode 26 .xip with an x86_64
    slice. Installed Xcode 26.6 RC2 side-by-side with 16.4 (NO OS upgrade), -downloadPlatform
    iOS for the iOS 26.5 SDK. The MTLTensorDomain/CADynamicRange link error is gone. (2) Signing
    reconciled (rename debt): make_profile for com.scottdix.neonsplice, fixed exportOptions,
    re-exported the stale Godot project -> neon_splice.xcodeproj, baked manual-signing xcargs
    into the Fastfile ship lane (beats Godot's Automatic-vs-Distribution conflict). (3) `fastlane
    ios ship` ran end-to-end -> FIRST BUILD ON TESTFLIGHT (ASC app id 6782516475). (4) Confirmed
    ../virtus-focus unaffected (Expo/EAS cloud, no local Xcode). (5) Docs corrected (CLAUDE.md,
    SESSION-005), 16.4 flagged for removal after on-device test. Glow/FPS still need a real device
    (now reachable via TestFlight).

# =============================================================================
# FOCUS — one milestone, one issue at a time
# =============================================================================
focus:
  active_milestone: "v0.1.0 - Proof of Concept"
  active_epic: "#1 — Phase 1-2: Foundation & Project Architecture"
  current_issue: "#11/#56 ± gates on stream volume + #55 Glow Battery + #51 finite level / finish line (rest of v0.2.0 core loop)"
  next_issue: "#53 enemy faction (real enemy types/behaviours) + #14 collision hardening as bullet counts scale"
  notes: >
    Session 7 DONE: #6 POC device-validated (glow + 60fps on iPhone 12 Pro); MVP slice shipped
    (#9 analog steer, #10/#52 always-on fire, partial #14) and feels good on device — see SESSION-007.
    Run scene (res://assets/levels/run.tscn) is the main scene; GameState autoload added (#2).
    Gates (#11/#56) mutate GameState.projectile_count (ALREADY wired to fleet fire volume), so the
    plumbing exists — gates just call GameState.add_projectiles(+/-N). #55 Glow Battery = a 0-100
    bar that drains on hazard/negative-gate, loss at 0. #51 finish line = finite distance + "RUN
    COMPLETE". REVIEW FLAG: damage model is now per-bullet-impact, NOT the locked D3 beam emitter
    (kept D3's perf intent; live bullets bounded ~140). Sanity-check before enemy/tier work scales
    counts. iOS ship over SSH: unlock keychain + set-key-partition-list first (see SESSION-007).
    TODO still open: remove Xcode-16.4.0.app (device test now passed, so it's safe to drop).

# =============================================================================
# TRACKER
# =============================================================================
# GitHub Issues is the canonical task tracker (gh CLI).
#   Epics:      type: epic   (one per implementation-plan phase, + enemy faction #53)
#   Milestones: v0.1.0 ... v1.0.0 (the version ladder)
#   Labels:     type / priority / effort / status / area
tracker:
  kind: github-issues
  repo: scottdix/neon-runner

# =============================================================================
# PROJECT REFERENCE
# =============================================================================
# Engine:  Godot 4.7 (STANDARD/non-mono build for the GDScript dev loop), GDScript-first
# Scope:   docs/design/GAME_SCOPE.md (authoritative — what the game is)
# Look:    docs/design/DESIGN_SPEC.md  ·  Plan/stubs: IMPLEMENTATION_PLAN.md (superseded appendix)
