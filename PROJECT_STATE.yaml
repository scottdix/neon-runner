schema_version: 1
# Solo project manifest — the session-memory heartbeat.
# Read at /session-start, advanced at /handoff. History depth is 2
# (notes + previous_notes); older history lives in git + docs/handoff/.
last_updated:
  date: "2026-06-23"
  session: 18
  notes: >
    Session 18 (DESIGN-ONLY — review of Patrick's TestFlight gameplay feedback; NO code, #23 untouched and
    still the coding focus). Turned 5 tester points into a depth pass on the combat/economy, backed by a
    5-agent Opus research dive (gate-runner genre, build-craft bullet-hells, roguelite economies, decision
    theory, game-feel) → NEW docs/design/GATE_ECONOMY.md (full reasoning + sources). NEW ISSUES: #78 Splice
    Lab economy (between-run RNG perk draft + enemy token drops), #79 gate-economy depth (Spray<->Lance
    stance + threat triangle — THE keystone; priority:high; written theme-agnostic), #80 Easy/Med/Hard
    difficulty, #81 ICEBOX/V2 twin-stick arena mode, #82 boss encounters (end-of-run climax, vary-only
    control) — #78/#79/#80/#82 all v0.5.0. COMMENTS: #42 weapon-state readability ("show the spread"),
    #78 in-run pickups = tokens only for v1, #59 absorbed the salvaged pacing concepts. CLEANUP: closed the
    stale adaptive-difficulty epic #29-33 (GAME_SCOPE D4 demoted it) — per-phase intensity knobs + rest
    zones folded into #59; #33 adaptive deaths/successes loop killed outright (player-facing difficulty is
    now #80). LOCKED: Spray/Lance discrete+flippable; Rhombus immunity scales with difficulty (hard=immune);
    meta horizontal not stat-creep; end-of-run boss first; bosses vary one-thumb control (no new schemes);
    in-run pickups = tokens. PARKED: the "math" theme reskin (kept out of #79's mechanic so it doesn't
    block), stance-flip cost, in-run consumables, debut boss (Singularity?). FOLLOW-UP: update GAME_SCOPE
    §4.2-4.4 to the stance model + elevated boss role — deferred until the math-theme question resolves.
  previous_notes: >
    Session 17 (DEVICE-FEEDBACK TRIAGE; #23 untouched, still next). Short session that CLOSED OUT the
    v0.3.0 (Visual Polish) milestone — now 0 open — and fixed a real device bug on build #13. Closed the
    three device-validation tails: #72 (in-run ship matches Garage — device-confirmed), #71 (grid restyle
    improved on device), #66 (AMOLED mode functionally correct + persists but subtle — BG_STANDARD is
    already near-black). NEW #76 — COMBO/battery readout was clipped by the iPhone 12/12 Pro notch; run.gd
    _safe_top_inset() (DisplayServer.get_display_safe_area() → canvas units) shifts the HUD top row below
    the top safe-area inset, 0 on notchless/headless; verify_scene PASS. NEW #77 — consolidated visual-
    polish backlog. NO new build shipped — user batched the #76 notch fix with #23 rather than ship build
    #14 for one cosmetic shift; confirm #76 on device whenever the next build goes out.

# =============================================================================
# FOCUS — one milestone, one issue at a time
# =============================================================================
focus:
  active_milestone: "v0.4.0 - Game Feel"
  active_epic: "#22 — Phase 5: Game Feel (Juice)"
  current_issue: "#23 — [Gameplay] Implement feedback manager (screen shake, flash). Wire the already-declared Events.trigger_screen_shake (+ a white-flash hook) into the Run scene as a FeedbackManager, consumed by HUD/camera. This is the v0.4.0 foundation the rest of Game Feel (combo/score-popups/milestones/audio) builds on."
  next_issue: "#24 — [Audio] Create audio manager with sound pooling (then #25 SFX for all game events, #26 combo system, #27 score popups, #28 milestone celebrations; #65 Haptics + #61 adaptive audio also live in v0.4.0)."
  notes: >
    Session 18 was DESIGN-ONLY (Patrick's TestFlight feedback → a v0.5.0 combat/economy depth pass; see NEW
    docs/design/GATE_ECONOMY.md + issues #78-82). The v0.5.0 depth cluster now has design intent: #79
    Spray<->Lance gate economy (keystone, priority:high), #78 Splice Lab token economy, #82 bosses, #80
    difficulty; the stale adaptive-difficulty epic #29-33 was closed (folded into #59). NO code this session
    — #23 is STILL UNTOUCHED and REMAINS the current coding focus. The "math" theme reskin is a PARKED open
    question (kept out of #79's mechanic); GAME_SCOPE §4.2-4.4 needs a sync once it resolves.
    START with #23 FeedbackManager: the Events bus already DECLARES trigger_screen_shake(intensity,duration)
    but NOTHING emits or consumes it — build a FeedbackManager in the Run scene that listens (camera offset
    shake + a white screen-flash) and have enemy_destroyed/enemy_breached/gate_passed/gate_hijack_blocked
    emit it. Then #24 audio manager + #25 SFX (audio/ dir is empty; AudioManager autoload planned in
    CLAUDE.md but not built), #26 combo visual feedback (GameState already tracks combo/combo_multiplier —
    surface it), #27 score popups, #28 milestone celebrations, #65 haptics (Haptics autoload exists — wire
    to feedback tiers), #61 adaptive audio.
    PENDING DEVICE-CHECK (no build shipped session 17 — user batched the #76 notch fix with #23 rather than
    ship build #14 for one cosmetic shift): confirm on the NEXT build that the COMBO/battery row clears the
    notch (#76). Build-#13 tail already confirmed: ship matches Garage, black band gone, grid restyle.
    DEPLOY is self-service: bump export_presets version → Godot iOS export (exit 1 expected) → fastlane ios
    ship (self-unlocks keychain via NEON_KEYCHAIN_PW) → status until VALID → beta_submit for external. NOTE:
    Apple ingestion can stall (build #12 was invisible >16 min — re-ship per the runbook if a build is
    invisible >25 min). Device backlog: glow/fonts/haptics/FPS only provable on iPhone (#47); polish backlog
    #77 (ship read, grid aesthetic, AMOLED contrast); #54/#34 the consolidated device-perf gate.
    OPS: rotate the keychain password (history scrubbed via git filter-repo + force-push, but rotation is the
    real fix); remove Xcode-16.4.0.app (rollback condition long met).

# =============================================================================
# TRACKER
# =============================================================================
# GitHub Issues is the canonical task tracker (gh CLI).
#   Epics:      type: epic   (one per implementation-plan phase, + enemy faction #53)
#   Milestones: v0.1.0 ... v1.0.0 (the version ladder)
#   Labels:     type / priority / effort / status / area
tracker:
  kind: github-issues
  repo: scottdix/neon-runner

# =============================================================================
# PROJECT REFERENCE
# =============================================================================
# Engine:  Godot 4.7 (STANDARD/non-mono build for the GDScript dev loop), GDScript-first
# Scope:   docs/design/GAME_SCOPE.md (authoritative — what the game is)
# Look:    docs/design/DESIGN_SPEC.md  ·  Plan/stubs: IMPLEMENTATION_PLAN.md (superseded appendix)
