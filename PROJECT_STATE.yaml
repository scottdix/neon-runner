schema_version: 1
# Solo project manifest — the session-memory heartbeat.
# Read at /session-start, advanced at /handoff. History depth is 2
# (notes + previous_notes); older history lives in git + docs/handoff/.
last_updated:
  date: "2026-06-23"
  session: 15
  notes: >
    Session 15 (CODE + SHIP). Built the ENTIRE v0.3.0 visual-identity layer via an ultracode Workflow
    (4 disjoint slices) + serialized headless verify, then shipped TestFlight build #11. Closed #16/#17
    (reactive grid: 8-ripple array + implosion + color-shift), #18 (MultiMesh neon trail — NOT Line2D,
    won't bloom) + #67 (engine exhaust; trail/engine apply in-run on loadout_changed), #19/#20 (pooled
    GPUParticles2D EffectLayer; revived dead spawn_particles signal), #57 (5-tier orb evolution + shatter),
    #68 (SpliceLab.active_modifiers() consumed by fleet at run start). Closed epic #15. #66 AMOLED kept
    open (code done, device-validation only). VERIFY: 4 new + 8 regression headless suites PASS incl
    verify_scene (real run.tscn 420 frames); caught+fixed leaked tool-tags in the grid shader.
    SHIP: bumped export_presets version, Godot iOS export, fastlane ios ship + beta_submit build:11 →
    VALID → IN_BETA_TESTING (external Friends&Family/Patrick + internal). SSH keychain-unlock now BAKED
    into the ship lane (NEON_KEYCHAIN_PW in gitignored fastlane/.env); full runbook in CLAUDE.md.
    SECURITY: scrubbed the keychain password from git history (git filter-repo + force-push); user will
    rotate the key later. Commits: 647a1ea (epic code), 90ebcc9 (deploy automation; post-rewrite SHA).
  previous_notes: >
    Session 14 (TRACKER RECONCILIATION — no gameplay code). Audited v0.1.0/v0.2.0/v0.3.0 against the
    actual codebase via subagent assessments, then closed/relocated issues to match reality. Recurring
    pattern: many "open" issues were already built or superseded but never closed.
    v0.1.0 PoC => COMPLETE: closed #1 (epic), #4 (GameState built; persistence in Settings.record_score,
    not save.json), #5 (ObjectPool SUPERSEDED — fleet/enemies are MultiMesh-batched, not node-pooled);
    fixed stale ObjectPool refs in project.godot + #62/#12.
    v0.2.0 Prototype => COMPLETE: closed #7 (epic), #12 (projectile mgr — built in fleet.gd), #14 (collision
    vs gates — superseded by ship-vs-gate + projectile-vs-enemy), #64 (Bazzite box — WON'T-DO, mac-mini +
    TestFlight covers device validation). Relocated device/feel-gated leftovers: #54 => v0.5.0 Mobile Opt
    (#34, device-perf only), #66 => v0.3.0 (#15), #65 => v0.4.0 (#22); target epic checklists updated.
    v0.3.0 Visual Polish (partial): closed 4 verified-done — #21 (neon styling), #49 (typography/color
    tokens), #58 (Glitch enemy + all archetypes), #62 (MultiMesh batching; 60fps gate consolidated under
    #54). REMAINING v0.3.0 is genuine unbuilt work (the visual identity), set as the next focus. Only
    project.godot (one comment) changed in the tree; everything else was GitHub issue bookkeeping.

# =============================================================================
# FOCUS — one milestone, one issue at a time
# =============================================================================
focus:
  active_milestone: "v0.4.0 - Game Feel"
  active_epic: "#22 — Phase 5: Game Feel (Juice)"
  current_issue: "#23 — [Gameplay] Implement feedback manager (screen shake, flash). Wire the already-declared Events.trigger_screen_shake (+ a white-flash hook) into the Run scene as a FeedbackManager, consumed by HUD/camera. This is the v0.4.0 foundation the rest of Game Feel (combo/score-popups/milestones/audio) builds on."
  next_issue: "#24 — [Audio] Create audio manager with sound pooling (then #25 SFX for all game events, #26 combo system, #27 score popups, #28 milestone celebrations; #65 Haptics + #61 adaptive audio also live in v0.4.0)."
  notes: >
    Session 15 COMPLETED all v0.3.0 visual-identity work (epic #15 closed; #16/#17/#18/#19/#20/#57/#67/#68
    closed) and shipped TestFlight build #11 (VALID → IN_BETA_TESTING for Patrick/external + internal). Focus
    now advances to v0.4.0 Game Feel (epic #22). START with #23 FeedbackManager: the Events bus already
    DECLARES trigger_screen_shake(intensity,duration) but NOTHING emits or consumes it — build a FeedbackManager
    in the Run scene that listens (camera offset shake + a white screen-flash) and have enemy_destroyed/
    enemy_breached/gate_passed/gate_hijack_blocked emit it. Then #24 audio manager + #25 SFX (audio/ dir is
    empty; AudioManager autoload was planned in CLAUDE.md but not built), #26 combo visual feedback (GameState
    already tracks combo/combo_multiplier — surface it), #27 score popups, #28 milestone celebrations, #65
    haptics (Haptics autoload exists — wire to feedback tiers), #61 adaptive audio.
    DEPLOY is now self-service: `fastlane ios ship` unlocks the keychain itself (NEON_KEYCHAIN_PW in gitignored
    fastlane/.env); full runbook in CLAUDE.md "Shipping to TestFlight". To ship: bump export_presets version →
    Godot iOS export (exit 1 is expected) → ship → status until VALID → beta_submit for external.
    PRIORITY device-check on build #11: it is the FIRST GPU render of ALL the session-15 visuals — confirm the
    neon actually reads (grid ripples, particles, trail/engine, tier punch, bloom). Device backlog unchanged:
    glow/fonts/haptics/FPS only provable on iPhone (#47); #66 AMOLED open as a device-validation tail; #54/#34
    is the consolidated device-perf gate. OPS: rotate the keychain password (history was scrubbed via
    git filter-repo + force-push, but rotation is the real fix); remove Xcode-16.4.0.app (rollback condition met).

# =============================================================================
# TRACKER
# =============================================================================
# GitHub Issues is the canonical task tracker (gh CLI).
#   Epics:      type: epic   (one per implementation-plan phase, + enemy faction #53)
#   Milestones: v0.1.0 ... v1.0.0 (the version ladder)
#   Labels:     type / priority / effort / status / area
tracker:
  kind: github-issues
  repo: scottdix/neon-runner

# =============================================================================
# PROJECT REFERENCE
# =============================================================================
# Engine:  Godot 4.7 (STANDARD/non-mono build for the GDScript dev loop), GDScript-first
# Scope:   docs/design/GAME_SCOPE.md (authoritative — what the game is)
# Look:    docs/design/DESIGN_SPEC.md  ·  Plan/stubs: IMPLEMENTATION_PLAN.md (superseded appendix)
