schema_version: 1
# Solo project manifest — the session-memory heartbeat.
# Read at /session-start, advanced at /handoff. History depth is 2
# (notes + previous_notes); older history lives in git + docs/handoff/.
last_updated:
  date: "2026-06-20"
  session: 4
  notes: >
    Session 4: Design-led planning (no gameplay code; user deferred real dev to
    next session). Imported the first art-direction pass from the Claude Design
    project "Ad-Free Gaterunner Game Design" (via DesignSync tool — WebFetch 403s)
    into docs/design/DESIGN_SPEC.md: palette (cyan #22e7ff / magenta #ff2bd6 mult /
    green #2bff9e add / orange #ff9d2b combo / gold-orb swarm), 4 fonts (Press Start
    2P, Orbitron, Rajdhani, Share Tech Mono), and 4 screens (Boot→Title→Run→Results).
    TWO CORE DECISIONS LOCKED, overriding IMPLEMENTATION_PLAN.md: (1) FINITE
    distance-based levels w/ FINISH line + "RUN COMPLETE" — NOT endless; (2) FIRING
    is a CORE mechanic — the orb swarm/fleet are projectiles the ship fires. Also:
    monetization = paymium (no ads, one-time IAP). New issues: #47 iOS deploy
    pipeline (POC), #48 boot, #49 type+color Theme, #50 IAP, #51 level/finish-line,
    #52 fleet firing. Re-scoped/commented: #44→RESULTS, #41/#42/#9, #29 (Phase6
    endless→per-level), #10/#12/#13. POC fast-path agreed: STANDARD (non-.NET)
    Godot build for POC, sign w/ paid Apple Dev account, iOS Simulator first then
    iPhone; only #6+#47 needed for first on-phone POC (#4/#5 deferred). #3 still
    NOT runtime-verified (carried from S3).
  previous_notes: >
    Session 3: First real code. (1) #2 DONE/closed — Godot project initialized:
    project.godot + full folder tree + icon.svg, calibrated for MOBILE iOS+Android
    PORTRAIT (base 1080x1920, Mobile renderer, hdr_2d for glow, ETC2/ASTC, 2x MSAA
    2D, touch emulation). (2) #3 STARTED (not closed) — autoload/events.gd written
    (14 signals) and registered as Events autoload; runtime verification BLOCKED.
    (3) Added CLAUDE.md. (4) Re-synced repo (FF to 8af23a7); HTTPS push works.
    BLOCKER: headless Godot HANGS at shutdown on this mac-mini and macOS here has no
    timeout/gtimeout (=> false passes). Do NOT validate via headless CLI — open the
    editor GUI or background-run-to-logfile+poll+pkill. project.godot mobile edits
    and the Events registration were NOT re-validated at runtime.

# =============================================================================
# FOCUS — one milestone, one issue at a time
# =============================================================================
focus:
  active_milestone: "v0.1.0 - Proof of Concept"
  active_epic: "#1 — Phase 1-2: Foundation & Project Architecture"
  current_issue: "#3 — Implement Events autoload (global event bus)"
  next_issue: "#6 — POC glow scene (+ #47 iOS deploy pipeline) — first on-phone POC"
  notes: >
    #3 still UNVERIFIED — first verify events.gd loads in the Godot editor GUI, then
    close it (the #47 'run on desktop' step also clears this). Agreed POC fast-path:
    #3 (verify) → #6 (POC glow scene, doubling as a MultiMesh stress test) + #47
    (iOS deploy pipeline) to get pixels on the iPhone/Simulator. #4 (GameState) and
    #5 (ObjectPool) are DEFERRED — not needed to render the glow POC. Use the
    STANDARD (non-.NET) Godot build for the POC; sign with the paid Apple Dev
    account; Simulator first, real iPhone second. Design + 2 core decisions:
    docs/design/DESIGN_SPEC.md (finite levels, firing core, paymium).

# =============================================================================
# TRACKER
# =============================================================================
# GitHub Issues is the canonical task tracker (gh CLI).
#   Epics:      type: epic   (one per implementation-plan phase)
#   Milestones: v0.1.0 ... v1.0.0 (the version ladder)
#   Labels:     type / priority / effort / status / area
tracker:
  kind: github-issues
  repo: scottdix/neon-runner

# =============================================================================
# PROJECT REFERENCE
# =============================================================================
# Engine:  Godot 4.3+ (.NET build), GDScript-first
# Plan:    IMPLEMENTATION_PLAN.md (7 phases, folder structure, code stubs)
