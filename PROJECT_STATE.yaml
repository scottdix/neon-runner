schema_version: 1
# Solo project manifest — the session-memory heartbeat.
# Read at /session-start, advanced at /handoff. History depth is 2
# (notes + previous_notes); older history lives in git + docs/handoff/.
last_updated:
  date: "2026-06-24"
  session: 23
  notes: >
    Session 23 (ULTRACODE): imported the cloud ultraplan combat-redesign POC (built on Claude-Code-web,
    no Godot there) via a git BUNDLE pulled over SSH from the MacBook Air -> review branch -> merged main;
    fixed its one Variant-infer lint + added tools/playtest_poc.gd integration harness. SHIPPED build #17
    (internal + external/Patrick) — the POC behind the Settings COMBAT (POC) selector (LEGACY/KINETIC/GEOM)
    — with a BUILD-SPECIFIC "What to Test" (the static-notes problem fixed; memories saved:
    ship-both-internal-external + testflight-what-to-test). DEVICE FEEDBACK on #17: Walled Gauntlet "feels
    good, keep it"; NO stance verdict yet (all three need refinement — can't FEEL the difference because
    enemies/gates too rough); wants "better gate choices + polished enemies". Ran a 4-agent TIGER TEAM
    (combat-systems / visual-language / arcade-dopamine / mobile-feasibility) on 7 proposed gate concepts ->
    synthesis + the GATE VISUAL LANGUAGE SYSTEM logged to #86/#88/#87 (bright-ring + dark negative-space
    core; color+silhouette=FAMILY, glyph=specific, edge/motion=RISK; stance-ghosting; gates avoid the enemy
    red/magenta band). PLANNED (Opus) then IMPLEMENTED the "make stance comparison FEELABLE" build via a
    19-agent workflow (serial production code -> parallel verifies -> 4-lens adversarial review -> fix):
    8 phases — (1) enemy readability (distinct per-archetype hues + Rhombus ENEMY_RHOMBUS_CORE armor tell +
    punchier flash), (2) gate-effect DISPATCH SEAM (new Events.gate_effect + O(1) Callable table in GameState;
    math gates via gate_passed untouched), (3) Geom Cache gate (new "fx" formation token), (4) phase-scoped
    Efficiency (-drain/-burst, resets on phase_changed) + GLOBAL Tungsten (Lance-weight, resets on start_run)
    through ONE fleet.hit_weight() seam — Tungsten reframed as weight-scaling NOT +1 pierce (bullets are a flat
    Array[Vector2]), (5) 5-family gate ring-visuals + wrong-stance ghosting, (6) stance pool-filtering
    (bias-not-drop off-allegiance), (7) STILL_SECS 0.2->0.13. ADVERSARIAL REVIEW caught 8 real bugs (2 blocker
    verify-crashes; the pool-filter SPRAY-allegiance NO-OP — read GameState.stance right after start_run resets
    it, now keyed off Settings.poc_mode; Efficiency dropping Lance below the 5.0 armor-crack floor -> split
    crack_weight() from hit_weight()) — all fixed. VALIDATION (main loop, not trusting the workflow): --import
    clean + 30/30 headless verifies PASS (+3 new: readability/gate_visual/gate_dispatch) + integration playtest
    PASS. Code-complete + verified; NOT yet shipped (handoff before ship). Build #17 is the live TestFlight build.
  previous_notes: >
    Session 22: device-tested the v0.5.0 build, shipped fix build #16 (TestFlight internal +
    external/Patrick), then PIVOTED to a combat/gate/stance DEPTH REDESIGN — research only, NO plan
    locked. Build #16 fixed the broken-on-device items from #15: boss PLAYABLE (HP bar + phase telegraph;
    persistent Spray/Lance stance gates flanking the arena; HUD stance indicator; Singularity gravity WIRED
    into live gameplay — was dead code; boss to upper third + drift; HP 7000->4500); grid scroll FLIPPED
    top->bottom; token magnetism a real PULL; perf overlay -> persisted Settings toggle. Independent --import
    clean + 25/25 verifies; added a verify_scene WIRING GUARD (fails if a boss/stance signal has no live
    consumer). THEN 3-lane research (Geometry Wars / gate mechanics / bullet-hell feel) ->
    docs/design/REDESIGN_RESEARCH.md + a reusable docs/design/STANCE_BRAINSTORM_PROMPT.md. FILED #86 (gate
    redesign — math gates bland), #87 (stance switching redesign — two-lane gate REJECTED, divide-by disliked),
    #88 (projectile richness), #85 (boss HP-bar layout). KEY VERDICT: Spray<->Lance + math-gate combat too
    shallow/bland — needs redesign, no direction chosen yet.

# =============================================================================
# FOCUS — one milestone, one issue at a time
# =============================================================================
focus:
  active_milestone: "v0.5.0 - Complete Game"
  active_epic: "Combat/gate/stance DEPTH REDESIGN. The 'make the stance comparison FEELABLE' build (#86 gate visual system + 4 gates + #88 enemy readability) is CODE-COMPLETE + 30/30 verified on the mini, NOT yet shipped. DEPENDENCY FLIP: #87 (lock stance) is BLOCKED on device feedback for #86+#88. Walled Gauntlet VALIDATED (keep). Separately: device-gated perf cluster #54/#39/#37/#36 under epic #34 still open."
  current_issue: "Ship build #18 + device-test the new combat surface, then LOCK the stance direction (#87). DEPLOY: bump export_presets.cfg 17->18, Godot iOS export (exit 1 expected), fastlane ios ship -> status VALID -> beta_submit build:18 version:0.1.0 for external; ship to BOTH testers with a BUILD-SPECIFIC 'What to Test' (memories: ship-both-internal-external + testflight-what-to-test). What-to-Test focus: enemy archetype hues (Glitch pink / Rhombus crimson+core / Fractal violet), 5 gate FAMILY rings + wrong-stance ghosting, Geom Cache (instant charge), Tungsten (Lance bites harder), Efficiency (sustain-vs-burst tradeoff), 0.13s brake feel. ON DEVICE judge: do the hues + family rings read at speed under bloom; is Tungsten/Efficiency dramatic; KINETIC vs GEOM verdict (#87). All glow/legibility/feel is iPhone-only."
  next_issue: "After the device read on #18: LOCK #87 (KINETIC vs GEOM vs hybrid). Then the NEXT build: Overclock Devil's-Bargain gate (rework '1 HP one-shot' -> drain-toward-1 + cancellable + can't self-kill; REUSES the phase-buff seam built this session; only loss channel = highest blast radius) + per-gate dark-core GLYPHS (the near-read layer, deferred from the visual MVP). PARKED new gate ideas: Avalanche (escalating-greed), Ironclad (Lance one-shots Rhombus/Spray=0), Phoenix (comeback), Threadneedle (graze), Mortgage (sacrifice->payoff). CUT (do not revive without cause): Hair-Trigger, Ricochet, Harvester. Quick deferred fix: perf overlay -> BOTTOM-RIGHT (#35). #85 boss HP-bar layout. Device-gated perf cluster #54/#39/#37/#36 + boss balance. #81 twin-stick stays ICEBOX/V2."
  notes: >
    Session 23 (ULTRACODE): shipped POC build #17 (live TestFlight build) then ran a tiger team + a 19-agent
    workflow to build the 'make the stance comparison FEELABLE' build — CODE-COMPLETE + 30/30 verified, NOT
    shipped (handoff first). Next session = ship #18 + device-test + lock #87. See last_updated.notes for the
    full session story.
    NEW SURFACE (session 23, uncommitted->committed this handoff): Events.gate_effect(id,params,at) — the
    generalized gate path; GameState._gate_effects Callable table + _on_gate_effect dispatcher + _fx_geom_cache/
    _fx_tungsten/_fx_efficiency handlers; GameState phase-scoped buffs geom_drain_mult/burst_damage_mult (reset
    on Events.phase_changed via _on_phase_changed/_reset_phase_buffs) + GLOBAL lance_hit_weight_mult (reset in
    start_run only); fleet.hit_weight() scales LANCE by lance_hit_weight_mult*burst_damage_mult, NEW
    fleet.crack_weight() (LANCE*Tungsten WITHOUT Efficiency) so armor-crack eligibility stays >= the 5.0 floor;
    Targets._apply_damage takes crack_weight separately; gate.gd Family enum + family field + _family_color/
    family ring textures + set_ghosted(); gate_spawner _stance_pool_filter/_allegiance_for_poc (allegiance from
    Settings.poc_mode NOT GameState.stance) + "fx" formation token via _configure_side; palette ENEMY_* distinct
    hues + ENEMY_RHOMBUS_CORE + GATE_FAMILY_* ; STILL_SECS=0.13. NEW verifies: verify_readability/gate_visual/
    gate_dispatch (30 total). tools/playtest_poc.gd is the integration harness.
    GOTCHAS still in force (all re-confirmed this session): a Node add_child()ed during a verify _initialize()
    has _ready DEFERRED under `-s` (await process_frame before asserting); a bare `Events`/`GameState`/`Settings`
    won't compile in the `-s` MAIN tool script (use root.get_node(...)); use runtime load() not top-level const
    preload() for scripts that reference autoload globals; any `var x := <Variant expr>` (Dict lookup/.call()/
    .get()) is a warning-treated-as-ERROR — type it explicitly; String(Color) is invalid (use str()); root has
    no get_root() (use root directly). Verify result FILES are hardcoded /tmp/verify_<name>_result.txt — pass
    that exact path to run-headless.sh. ALWAYS re-run --import + the verify sweep in the MAIN loop, never trust
    the workflow's self-checks (the adversarial review's per-agent --import missed 8 bugs incl 2 blocker
    verify-crashes + a feature no-op).
    DEPLOY unchanged + proven: bump export_presets version (17->18 next) -> Godot iOS export (exit 1 EXPECTED) ->
    fastlane ios ship (self-unlocks keychain via NEON_KEYCHAIN_PW) -> status until VALID -> beta_submit build:N
    version:0.1.0 for external. ALWAYS set a build-specific What-to-Test (whatsnew:) and ship to BOTH testers.
    Re-ship if a build is invisible >25 min (Apple ingestion stall).
    DEVICE BACKLOG (iPhone-only): the WHOLE session-23 build (enemy hues / gate family rings / ghosting / Tungsten
    / Efficiency / 0.13 brake) — does it read at speed under bloom + is the stance comparison feelable (the #87
    decision); plus session-21 FEEL (stance flips, boss arm/gravity, perk draft, phase crescendo), #76 notch,
    glow/fonts/real-FPS, the #34 perf cluster (#54/#39/#37/#36), #77.
    OPS: rotate the keychain password; remove Xcode-16.4.0.app (rollback condition long met).

# =============================================================================
# TRACKER
# =============================================================================
# GitHub Issues is the canonical task tracker (gh CLI).
#   Epics:      type: epic   (one per implementation-plan phase, + named epics: #53 enemy faction [CLOSED],
#               #84 combat & economy depth [v0.5.0])
#   Milestones: v0.1.0 ... v1.0.0 (the version ladder)
#   Labels:     type / priority / effort / status / area
tracker:
  kind: github-issues
  repo: scottdix/neon-runner

# =============================================================================
# PROJECT REFERENCE
# =============================================================================
# Engine:  Godot 4.7 (STANDARD/non-mono build for the GDScript dev loop), GDScript-first
# Scope:   docs/design/GAME_SCOPE.md (authoritative — what the game is)
# Look:    docs/design/DESIGN_SPEC.md  ·  Plan/stubs: IMPLEMENTATION_PLAN.md (superseded appendix)
