schema_version: 1
# Solo project manifest — the session-memory heartbeat.
# Read at /session-start, advanced at /handoff. History depth is 2
# (notes + previous_notes); older history lives in git + docs/handoff/.
last_updated:
  date: "2026-06-20"
  session: 3
  notes: >
    Session 3: First real code. (1) #2 DONE/closed — Godot project initialized:
    project.godot + full folder tree + icon.svg, calibrated for MOBILE iOS+Android
    PORTRAIT (base 1080x1920, Mobile renderer, hdr_2d for glow, ETC2/ASTC, 2x MSAA
    2D, touch emulation). Orientation = portrait, user-confirmed (overrides the
    plan's landscape grid-shader hint — flip that shader's vec2(1920,1080) when
    built). (2) #3 STARTED (not closed) — autoload/events.gd written (14 signals)
    and registered as Events autoload; runtime verification BLOCKED. (3) Added
    CLAUDE.md (architecture + PM workflow + gotchas). (4) Re-synced repo (was 1
    commit behind; FF to 8af23a7); HTTPS push works, no key changes needed.
    BLOCKER for next session: headless Godot HANGS at shutdown on this mac-mini
    (--editor/--import/--script all do their work but never exit) and macOS here
    has no timeout/gtimeout (=> false passes). Do NOT validate via headless CLI —
    open the editor GUI or background-run-to-logfile+poll+pkill. project.godot
    mobile edits and the Events registration were NOT re-validated at runtime;
    first action next session = open in GUI and confirm clean load + Events autoload.
  previous_notes: >
    Session 2: Migrated primary development to the mac-mini (user scottdix) — the
    headless dev box; Claude Code runs natively there. Provisioned userspace/
    no-Homebrew: repo at ~/Documents/neon-runner, Godot 4.7-mono symlinked at
    ~/.local/bin/godot, .NET SDK 8.0.422, PATH+DOTNET_ROOT in ~/.zshrc. Xcode 16.4
    (iOS Sim), Android emulator/adb, node, gh, java already present. gh authed as
    scottdix (ADMIN), HTTPS push works. Stack stays Godot/.NET. No project code yet
    — #2 not started. The mac-mini's Claude has its own ~/.claude and won't see the
    laptop's memory — the repo (PROJECT_STATE + docs/handoff) is the only cross-box
    context.

# =============================================================================
# FOCUS — one milestone, one issue at a time
# =============================================================================
focus:
  active_milestone: "v0.1.0 - Proof of Concept"
  active_epic: "#1 — Phase 1-2: Foundation & Project Architecture"
  current_issue: "#3 — Implement Events autoload (global event bus)"
  next_issue: "#4 — Implement GameState autoload (score, multiplier, settings)"
  notes: >
    #3 is partially done (events.gd written + registered) but UNVERIFIED — first
    verify it loads in the Godot editor GUI, then close it. Epic order:
    #3 (Events) → #4 (GameState) → #5 (ObjectPool) → #6 (POC glow scene,
    expanded into a MultiMesh stress test per the Session 1 engine decision).

# =============================================================================
# TRACKER
# =============================================================================
# GitHub Issues is the canonical task tracker (gh CLI).
#   Epics:      type: epic   (one per implementation-plan phase)
#   Milestones: v0.1.0 ... v1.0.0 (the version ladder)
#   Labels:     type / priority / effort / status / area
tracker:
  kind: github-issues
  repo: scottdix/neon-runner

# =============================================================================
# PROJECT REFERENCE
# =============================================================================
# Engine:  Godot 4.3+ (.NET build), GDScript-first
# Plan:    IMPLEMENTATION_PLAN.md (7 phases, folder structure, code stubs)
