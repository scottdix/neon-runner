schema_version: 1
# Solo project manifest — the session-memory heartbeat.
# Read at /session-start, advanced at /handoff. History depth is 2
# (notes + previous_notes); older history lives in git + docs/handoff/.
last_updated:
  date: "2026-06-25"
  session: 25
  notes: >
    Session 25 (ULTRACODE): THE PIVOT — Neon Splice is now a twin-lane HORDE survival shooter, LOCKED as the
    core game (mode selector removed; Settings.poc_mode forced to HORDE; LEGACY/KINETIC/GEOM parked + reversible).
    Built across 2 sequential-build+adversarial-review workflows: (A) H0-H5 — permanent center divider as a FIRING
    BOUNDARY (lane_arena.gd; ship roams full width, only damages its near side of CENTER_X=540), ramping fodder
    both lanes (targets.gd _spawn_horde), 20s lane-boss KIND_LANEBOSS (separate spawn path = boss-exclusion is
    free), FIREPOWER-AS-HEALTH (breach -> GameState.drain_firepower(streams) drains projectile_count, lose at 0;
    HUD firepower bar). (B) batch — thin CYAN divider + HOLLOW neon enemies (transparent-core texture; Glitch
    hot-pink / Rhombus neon-green / Fractal violet via palette ENEMY_*), Debug autoload + pause Debug menu, +/×
    gates. New autoload/debug.gd (tokens/enemies/gates toggles; density/speed/strength/firepower_loss/enemy_cap
    steppers, density+cap UNBOUNDED, MultiMesh buffer 1024 so cap pushes past 256; 3 bullet-passthrough
    placeholders; persists user://debug.cfg; null-safe accessors). Debug knobs wired into the FODDER path only
    (boss untouched) + firepower_loss in _breach. Gates: data/level_horde.tres +/×-only schedule + gate_spawner
    _restrict_op; enemies ignore gates in HORDE (_maybe_multiply/hijack suppressed). TWO DEVICE BUGS fixed in the
    main loop: Debug menu rendered all rows on one line (GDScript lambda captured the Y cursor by value -> instance
    member + _add_row method; confirmed via tools/shot_debug_menu.gd PNG); firepower bar never moved
    (_relabel_battery_as_firepower defined but NEVER CALLED in run.gd HORDE setup -> added the call). VALIDATION:
    the workflow self-checks MISSED 10 stale verifies (session-23 lesson AGAIN) -> ran the full sweep in the main
    loop, reconciled all 10 (force-LEGACY for parked paths / color-intent updates; a 3rd workflow, all real_bug=none)
    -> 42/42 PASS + --import clean. SHIPPED build #19 (both testers) — first HORDE build on a device. HORDE epic = #89
    (code comments say #90, a planning guess); #86/#87/#88 commented superseded/parked/partial.
  previous_notes: >
    Session 24: SHIPPED build #18 (internal + external/Patrick) — first time the session-23 "feelable"
    combat surface reaches testers — with a build-specific What-to-Test; clean beta_submit (no spaceship
    description-bug). Knocked out the one genuinely headless-doable v0.5.0 task: #37 particle budget WIRING
    (effect_layer._emit now consults ParticleBudget.grant() with per-emitter live-count accounting + decay;
    was a unit-tested island) + new verify_particles budget-seam asserts; 30/30 verifies + playtest PASS.
    #35 perf overlay re-anchored BOTTOM-RIGHT (right-aligned, 140px inset clears the home indicator);
    verify_perf PASS. Ran a 6-agent scope workflow that correctly found 5 of 6 open v0.5.0 issues are
    DEVICE-GATED with code already done — flagged all 6 status:blocked with honest comments + surfaced the
    #85 stale-premise. Root-caused the enemy "no visual difference" complaint (#88): all archetypes share
    ONE soft-diamond mesh (differ only by size + HDR color) and additive+bloom washes the hues to white —
    distinct SILHOUETTES were never built. BIGGEST WIN: fixed the blind-visual loop — installed Godot 4.7
    (same build) on the M2 MacBook Air, wrote tools/sync-to-air.sh (~12s rsync+reimport), and CONFIRMED the
    game runs on the Air via Metal/M2 with REAL glow + ZERO compute-pipeline errors (the mini's hard
    blocker). Documented in CLAUDE.md + memory mini-air-glow-preview. Researched the design workflow
    (Gemini/AI Studio mockup -> Claude spec -> Claude Code impl) + reference repos (PerfBullets/BlastBullets2D,
    godotshaders). All committed this handoff.

# =============================================================================
# FOCUS — one milestone, one issue at a time
# =============================================================================
focus:
  active_milestone: "v0.5.0 - Complete Game"
  active_epic: "#89 HORDE mode — twin-lane survival is THE core game (Neon Splice). BUILT + SHIPPED (build #19, both testers): firing-boundary center divider, ramping fodder both lanes + 20s lane-boss, FIREPOWER-AS-HEALTH + +/× recovery gates, HOLLOW neon enemies, pause DEBUG menu (autoload/debug.gd). Mode selector removed; LEGACY/KINETIC/GEOM parked (reversible). OPEN/device-gated: real-iPhone FRAMERATE at the 256 enemy cap (#1 unknown — Patrick's read) + neon readability under bloom + lose/recover tuning. Supersedes #86 (gates -> +/× firepower), #87 (stance parked), #88 (partial — hues done, silhouettes deferred). mini->Air glow loop is the iteration surface; perf cluster #54/#39/#37/#36 under #34 still open (status:blocked)."
  current_issue: "PATRICK's DEVICE READ on build #19 (HORDE, #89) -> then TUNE. #1 unknown = FRAMERATE with the swarm at MAX_ENEMIES soft-cap 256 on a real iPhone (the batched consume_volumes pass + MultiMesh(1024 buffer) + per-kill token drops all scale) — dial the cap/density DOWN if FPS suffers. Then: readability of the hollow neon enemies under bloom (Glitch hot-pink / Rhombus neon-green / Fractal violet) + the thin cyan divider; and tune the lose/recover loop — DENSITY / SPEED / STRENGTH / FIREPOWER-LOSS / ENEMY-CAP / +-× gate values — ALL live via the pause DEBUG menu (autoload/debug.gd, persisted user://debug.cfg). Iterate on the Air (real M2 bloom) first, confirm on device. The Debug-menu render + firepower-bar bind were DEVICE BUGS fixed this session (lambda-captured-Y / _relabel_battery_as_firepower never called)."
  next_issue: "After the #19 device read + tuning: per-archetype enemy SILHOUETTES (#88 — all archetypes still share ONE diamond texture; give Glitch/Rhombus/Fractal distinct hollow outlines on the MultiMesh path). Wire the 3 BULLET-PASSTHROUGH placeholders to real behavior (bullets pass through weak enemies by strength). Device-gated perf cluster #54/#39/#37/#36 (closes on a real FPS pass — exactly what the 256-cap horde stresses). Possible cleanup: collapse the parked LEGACY/KINETIC/GEOM modes + their force-LEGACY verifies if HORDE is made permanent. #81 twin-stick stays ICEBOX/V2."
  notes: >
    Session 25: HORDE is the core game (full story in last_updated.notes). NEW SURFACE: autoload/debug.gd
    (Debug autoload after Settings; persisted user://debug.cfg; null-safe tokens_on/enemies_on/gates_on/
    density_mult/speed_mult/strength_mult/firepower_loss/cap; Events.debug_changed) + assets/ui/debug_menu.gd
    (pause-overlay menu; UI.toggle_row/UI.stepper_row in ui_kit). assets/obstacles/lane_arena.gd (permanent
    divider; firing-boundary side_of/CENTER_X=540). targets.gd: KIND_LANEBOSS + STATS streams; _spawn_horde
    fodder path (set_horde) reads Debug density/speed/strength/enemy_cap; far-side filter; _make_diamond_texture
    HOLLOW; MultiMesh buffer 1024; _maybe_multiply/hijack suppressed in HORDE; _breach -> drain_firepower *
    Debug.firepower_loss. game_state.gd: HORDE_START_FIREPOWER=40, drain_firepower, _load_level -> level_horde.tres.
    run.gd: LaneArena + set_horde + _relabel_battery_as_firepower (firepower HUD) in HORDE; gates only if
    Debug.gates_on(). gate_spawner _restrict_op (+/x only). settings.gd: poc_mode FORCED HORDE + COMBAT(POC)
    selector removed. palette ENEMY_* recolored (Glitch pink / Rhombus green / Fractal violet, all hollow).
    AIR LOOP (iteration surface): edit on mini -> tools/sync-to-air.sh -> run on Air ~/.local/bin/godot --path
    ~/Documents/neon-runner res://assets/ui/boot.tscn (real M2 bloom); tools/shot_debug_menu.gd PNG-captures
    flat UI for layout. The mini renders no bloom/FPS.
    GOTCHAS still in force: a Node add_child()ed during a verify _initialize()
    has _ready DEFERRED under `-s` (await process_frame before asserting); a bare `Events`/`GameState`/`Settings`
    won't compile in the `-s` MAIN tool script (use root.get_node(...)); use runtime load() not top-level const
    preload() for scripts that reference autoload globals; any `var x := <Variant expr>` (Dict lookup/.call()/
    .get()) is a warning-treated-as-ERROR — type it explicitly; String(Color) is invalid (use str()); root has
    no get_root() (use root directly). Verify result FILES are hardcoded /tmp/verify_<name>_result.txt — pass
    that exact path to run-headless.sh. ALWAYS re-run --import + the verify sweep in the MAIN loop, never trust
    the workflows' self-checks (this session they MISSED 10 stale verifies + 2 device bugs the headless
    suite can't see). GDScript LAMBDAS capture locals BY VALUE — a captured running cursor never advances (use
    an instance member); this caused the Debug-menu all-rows-on-one-line bug.
    DEPLOY unchanged + proven: bump export_presets version (19->20 next) -> Godot iOS export (exit 1 EXPECTED) ->
    fastlane ios ship (self-unlocks keychain via NEON_KEYCHAIN_PW) -> status until VALID -> beta_submit build:N
    version:0.1.0 for external. ALWAYS set a build-specific What-to-Test (whatsnew:) and ship to BOTH testers.
    Re-ship if a build is invisible >25 min (Apple ingestion stall).
    DEVICE BACKLOG (iPhone-only): HORDE build #19 — FRAMERATE at the 256 cap (the gate), neon readability under
    bloom, lose/recover feel; plus glow/fonts/real-FPS, the #34 perf cluster (#54/#39/#37/#36), #76 notch, #77.
    OPS: rotate the keychain password; remove Xcode-16.4.0.app (rollback condition long met).

# =============================================================================
# TRACKER
# =============================================================================
# GitHub Issues is the canonical task tracker (gh CLI).
#   Epics:      type: epic   (one per implementation-plan phase, + named epics: #53 enemy faction [CLOSED],
#               #84 combat & economy depth [v0.5.0])
#   Milestones: v0.1.0 ... v1.0.0 (the version ladder)
#   Labels:     type / priority / effort / status / area
tracker:
  kind: github-issues
  repo: scottdix/neon-runner

# =============================================================================
# PROJECT REFERENCE
# =============================================================================
# Engine:  Godot 4.7 (STANDARD/non-mono build for the GDScript dev loop), GDScript-first
# Scope:   docs/design/GAME_SCOPE.md (authoritative — what the game is)
# Look:    docs/design/DESIGN_SPEC.md  ·  Plan/stubs: IMPLEMENTATION_PLAN.md (superseded appendix)
