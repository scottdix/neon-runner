schema_version: 1
# Solo project manifest — the session-memory heartbeat.
# Read at /session-start, advanced at /handoff. History depth is 2
# (notes + previous_notes); older history lives in git + docs/handoff/.
last_updated:
  date: "2026-06-20"
  session: 2
  notes: >
    Session 2: Migrated primary development to the mac-mini (ssh mac-mini, user
    scottdix) — it becomes the headless dev box, and Claude Code runs NATIVELY
    there inside a user-started tmux session (the M2 laptop is out of the loop).
    Provisioned it userspace/no-Homebrew: repo cloned at ~/Documents/neon-runner,
    Godot 4.7-mono at ~/Applications/Godot_mono.app (symlinked ~/.local/bin/godot,
    runs headless), .NET SDK 8.0.422 at ~/.dotnet, PATH+DOTNET_ROOT in ~/.zshrc.
    Already had Xcode 16.4 (iOS Sim), Android emulator/adb, node, gh, java.
    Credentials verified: gh authed as scottdix (ADMIN; repo/workflow/gist/read:org),
    wired as git credential helper, HTTPS push works. Stack confirmed: stay
    Godot/.NET (EAS/emulator tooling was incidental). No project code yet — #2 not
    started. ON THE MAC-MINI: git pull first, then /session-start. The mac-mini's
    Claude has its own ~/.claude and won't see the laptop's memory — the repo
    (PROJECT_STATE + docs/handoff) is the only cross-box context.
  previous_notes: >
    Session 1: Project-management bootstrap + an engine/performance design
    decision. (1) Stood up a stripped-down solo PM layer modeled on the
    waypoint-housing-portal system: PROJECT_STATE.yaml + docs/handoff/ +
    three skills (/session-start, /handoff, /plan). GitHub Issues is the tracker
    (46 issues, 7 phase epics, 6 milestones). (2) DECISION: do NOT build a custom
    engine a la Geometry Wars. The mobile concern (large/growing entity counts) is
    a batching problem, not an engine problem — solve in Godot via
    MultiMeshInstance2D, GPUParticles2D, full-screen bloom (cost ~constant), and
    treating the projectile swarm as cosmetic followers of ONE logical blob. The
    POC glow scene (#6) should double as a MultiMesh stress test on a real
    mid-range phone before committing to Phase 3+.

# =============================================================================
# FOCUS — one milestone, one issue at a time
# =============================================================================
focus:
  active_milestone: "v0.1.0 - Proof of Concept"
  active_epic: "#1 — Phase 1-2: Foundation & Project Architecture"
  current_issue: "#2 — Initialize Godot 4.x project with folder structure"
  next_issue: "#3 — Implement Events autoload (global event bus)"
  notes: >
    Phase 1-2 foundation work. Order within the epic: #2 (init project) →
    #3 (Events) → #4 (GameState) → #5 (ObjectPool) → #6 (POC glow scene,
    expanded into a MultiMesh stress test per the Session 1 engine decision).

# =============================================================================
# TRACKER
# =============================================================================
# GitHub Issues is the canonical task tracker (gh CLI).
#   Epics:      type: epic   (one per implementation-plan phase)
#   Milestones: v0.1.0 ... v1.0.0 (the version ladder)
#   Labels:     type / priority / effort / status / area
tracker:
  kind: github-issues
  repo: scottdix/neon-runner

# =============================================================================
# PROJECT REFERENCE
# =============================================================================
# Engine:  Godot 4.3+ (.NET build), GDScript-first
# Plan:    IMPLEMENTATION_PLAN.md (7 phases, folder structure, code stubs)
